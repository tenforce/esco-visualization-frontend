`import languages from '../../../utils/languages'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | languages'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = languages()
  assert.ok result
